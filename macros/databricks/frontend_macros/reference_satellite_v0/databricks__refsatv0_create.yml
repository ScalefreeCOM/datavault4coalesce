fileVersion: 1
id: "92"
macroString: |-
  {#-- Utility macro: outputs a SQL function to create a Reference Satellite V0. --#}
  {%- macro datavault4coalesce__databricks_reference_satellite_v0_create(node, columns) -%}

    {{ stage('Create Satellite Table') }}

    CREATE OR REPLACE TABLE {{ ref_no_link(node.location.name, node.name) }}
    (
      {% for col in columns %}
        `{{ col.name }}` {{ col.dataType }}
        {%- if not col.nullable %} NOT NULL
          {%- if col.defaultValue | length > 0 %} DEFAULT {{ col.defaultValue }}{% endif %}
        {% endif %}
        {%- if col.description | length > 0 %} COMMENT '{{ col.description | escape  }}'{% endif %}
        {%- if not loop.last -%}, {% endif %}
      {% endfor %}
    )
    {%- if node.description | length > 0 %} COMMENT = '{{ node.description | escape  }}'{% endif %}
  {%- endmacro -%}
name: databricks__refsatv0_create
type: Macro