fileVersion: 1
id: "4-run-databricks"
macroString: |- 
  {#-- Utility macro: outputs a SQL function to run a stage table. --#}
  {%- macro datavault4coalesce__databricks_stage_run(node, columns) -%}

    {% if node.override.run.enabled %}
      {{ node.override.run.script }}
    {% endif %} 
  {%- endmacro -%}
name: datavault4coalesce__databricks_stage_run
type: Macro