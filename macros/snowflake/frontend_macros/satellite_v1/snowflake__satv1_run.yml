fileVersion: 1
id: "datavault4coalesce__snowflake_satellite_v1_run" 
macroString: |- 
  {#-- Utility macro: outputs a SQL function to run a satellite table. --#}
  {%- macro datavault4coalesce__snowflake_satellite_v1_run(node, columns) -%}

    {% if node.override.run.enabled %}
      {{ node.override.run.script }}
    {% endif %} 
  {%- endmacro -%}
name: datavault4coalesce__snowflake_satellite_v1_run
type: "Macro"
